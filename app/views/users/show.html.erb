<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
  <div class="col-md-8">
    <%= render 'follow_form' if logged_in? %>
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts"><%= render @microposts %></ol>
      <ol class="microposts"><a href="https://techacademy-umemurayuka.c9users.io/users/1" target="_blank">フォロワー</a></ol>
      <ol class="microposts"><a href="example.html" target="_blank">フォロー</a></ol>
    <% end %>
  </div>
</div>